How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?
You can find the bugger that ****ed it up

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like
Google docs does? It can be commited when fixed or working not partially
complete

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file
separately? Dependency's of each file in relation to the change. Free will

How can you use the commands git log and git diff to view the history of
files? By getting the commit serial numbers from the git log for the two
commits to compare by diffing them

How might using version control make you more confident to make changes that
could break something? It will allow you to trace the commit that inroduced
the bug and blame the appropriate person.

Now that you have your workspace set up, what do you want to try using Git
for? The churn

What happens when you initialize a repository? Why do you need to do it?
Initialise creates a new repository you do this to create the .git files of
meta data.
